"""Example DGPs illustrating the ``dgp_protocol`` surface.

Marker file: declares ``examples/`` as a Python package so individual
example modules can import each other (e.g.,
:mod:`examples.coin_plus_noise` composes :mod:`examples.fair_coin`).
Each example module is still independently runnable as a script::

    python examples/fair_coin.py
    python examples/coin_plus_noise.py

and ``%run`` / ``ipython -i`` work as before.  Examples remain
illustrative; they are not part of the public ``dgp_protocol`` API.
"""
